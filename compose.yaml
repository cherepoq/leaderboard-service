services:
  postgres:
    image: postgres:16
    container_name: leaderboard_postgres
    environment:
      POSTGRES_DB: leaderboard
      POSTGRES_USER: app
      POSTGRES_PASSWORD: app
    ports:
      - "5432:5432"
